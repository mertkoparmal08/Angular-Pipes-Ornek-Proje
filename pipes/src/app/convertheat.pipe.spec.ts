import { ConvertheatPipe } from './convertheat.pipe';

describe('ConvertheatPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertheatPipe();
    expect(pipe).toBeTruthy();
  });
});
